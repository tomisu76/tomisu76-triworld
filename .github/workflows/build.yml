name: TriWorld Build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --silent
      - name: Test
        id: tests
        continue-on-error: true
        run: npm test -- --reporter=json --outputFile=test-results.json
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v4-road-test-results
          path: test-results.json
          if-no-files-found: error
      - name: Enforce test result
        if: steps.tests.outcome == 'failure'
        run: exit 1
      - name: TypeScript typecheck
        run: npx tsc --noEmit
      - name: Web production build
        run: npx vite build
      - name: BeamNG road-first ZIP
        run: npm run beamng:v4:road
      - name: Upload BeamNG package
        uses: actions/upload-artifact@v4
        with:
          name: triworld-v4-road-first
          path: |
            dist/triworld_v4_road_first.zip
            dist/triworld_v4_road_first.manifest.json
          if-no-files-found: error
